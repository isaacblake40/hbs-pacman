# HBS Pac-Man — Stage 0: Project Scaffold

This is the very first stage: just getting a working, empty Next.js app running. No Pac-Man game code yet.

## What will happen

1. **Check tools** — confirm Node.js, npm, and Git are installed. If any are missing, stop and give exact download links/steps instead of guessing.
2. **Scaffold the app** — run `npx create-next-app@latest .` directly at the root of this existing repo (`/home/user/hbs-pacman`), using:
   - TypeScript
   - ESLint
   - Tailwind CSS
   - `src/` directory
   - App Router
   - npm as the package manager
3. **No game code, no branding tweaks** — the starter page stays 100% stock Next.js boilerplate. Confirmed with you: no title changes, no content changes.
4. **Git** — the repo already exists and is on branch `claude/compassionate-bardeen-sh4q10` with no commits. I'll add the generated files and make one clean initial commit.
5. **Run it** — start the dev server (`npm run dev`) and verify the starter page loads without console/build errors (via HTTP check and/or a screenshot).

## What you'll get at the end

- A working Next.js + TypeScript + Tailwind + App Router project at the repo root.
- One clean initial git commit.
- Confirmation the dev server boots and the starter page renders with no errors.
- A plain-English summary of exactly what was created/changed.

## What's explicitly out of scope for this stage

- No Pac-Man game logic, assets, grid, sprites, or controls.
- No custom styling or branding beyond the Next.js default starter.
- No deployment setup.

---
**Next step after your approval:** I'll run the tool checks, scaffold the app, commit, and start the dev server — then pause again before Stage 1 (actual game development).
